[mysqld]
server-id  = 2
relay-log  = relay-bin
read-only  = 1

# Do NOT use replicate-do-db — it silently skips DDL statements
# (CREATE DATABASE, CREATE TABLE) that arrive without a USE context.
# All replication filtering is handled by the master's binlog instead.
